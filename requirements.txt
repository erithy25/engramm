# Exakte Pins — Referenzumgebung wie im dokumentierten R6-Messprotokoll
# (Apple M4, macOS, Python 3.13): NumPy 2.4.1. Installation:
#   python3.13 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
numpy==2.4.1
pytest==9.1.1
