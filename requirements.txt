# Direkte Abhaengigkeiten, exakt gepinnt.
# Referenzumgebung siehe docs/PROTOCOL.md (Apple M4, macOS, Python 3.13).
# Vollstaendiger Abzug inkl. transitiver Pakete: requirements.lock
#
#   python3.13 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.lock        # exakte Reproduktion
#   pip install -r requirements.txt         # nur direkte Pins
numpy==2.4.1
scikit-learn==1.9.0
pytest==9.1.1
