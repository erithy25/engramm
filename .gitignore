# Daten-Cache und Laufzeit-Artefakte — nie committen
# (data/loaders.py ist Code und wird versioniert; nur der Download-Cache nicht)
data/cache/
logs/

# Python
__pycache__/
*.pyc

# virtuelle Umgebungen
venv/
.venv*/

# macOS
.DS_Store

# results/ wird BEWUSST NICHT ignoriert: Benchmark-Ergebnis-JSONs
# (Zeitstempel, Seed, Commit-Hash, Umgebung) gehoeren ins Repo.
